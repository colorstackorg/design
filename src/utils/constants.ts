export enum MediaQuery {
  DESKTOP = '@media (min-width: 1025px)',
  DESKTOP_LARGE = '@media (min-width: 1441px)',
  MOBILE = '@media (max-width: 568px)',
  TABLET = '@media (max-width: 1024px) and (min-width: 569px)',
  TABLET_MAX = '@media (max-width: 1024px)'
}

export enum Color {
  BLACK = '#000000',
  ERROR = '#F02834',
  GOLD = '#FCB432',
  GRAY = '#C4C4C4',
  SUCCESS = '#45BD62',
  TEAL = '#038575',
  WHITE = '#FFFFFF'
}

export enum Size {
  XXS = 4,
  XS = 8,
  SS = 12,
  SM = 16,
  SL = 20,
  MD = 24,
  LG = 32,
  XL = 40,
  XXL = 48
}
